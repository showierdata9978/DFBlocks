on:
  push:
     branches:
        - main

jobs: 
  publish:
    runs-on: ubuntu-latest
    steps:
     - name: Publish to pypi
       uses: coveooss/pypi-publish-with-poetry@1.0.0
       with:
         project-name: DFBlock
         pypi-token: ${{ secrets.PYPI_TOKEN }}
